using System;
using System.Collections.Generic;
using System.Text;

namespace Basic.MinValue
{
    /// <summary>
    /// Identify the closest/smallest gap (a whole number) between the adjacent two numbers entered until the end of line is reached. Print out -1 if less than two numbers are entered.
    /// </summary>
    public class Closest
    {
        public static void Main()
        {
            // Write codes to enter a sequence of integers until the end of line.
            // In the end, print out the smallest difference between the two adjacent integers (in integer).
        }
    }
}

using System;
using System.Collections.Generic;
using System.Text;

namespace Basic.BooleanIndicator
{
    /// <summary>
    /// Use a Boolean indicator to alternate the sum and difference of a number sequence until it reaches the end of the line.
    /// </summary>
    /// <example>
    /// If you have entered 1, 2, 3, 4, 5; the program will calculate 1 - 2 + 3 - 4 + 5 = 3.
    /// </example>

    public class AlternateSum
    {
        public static void Main()
        {
            // declare and initialize a boolean variable first.
            bool alternateSum;
            // write codes to enter a sequence of numbers until the end of line is reached.
            // Then use the alternateSum boolean variable (alternateSum = !alternateSum) to indicate whether you should + or - the number.
        }

    }
}

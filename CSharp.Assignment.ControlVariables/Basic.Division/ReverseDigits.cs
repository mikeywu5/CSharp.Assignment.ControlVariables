using System;

namespace Basic.Division
{
    /// <summary>
    /// Print out all the digits of a positive number entered in a reversed order on the same line.
    /// </summary>
    public class ReverseDigits
    {
        public static void Main()
        {
            // first, enter an integer.
            // then, reverse the digits by reducing the digits in the integer through divisions.
        }
    }
}

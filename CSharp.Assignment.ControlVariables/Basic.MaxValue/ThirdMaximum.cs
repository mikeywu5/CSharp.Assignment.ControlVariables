using System;
using System.Collections.Generic;
using System.Text;

namespace Basic.MaxValue
{
    /// <summary>
    /// Print out the third largest number entered from a sequence of at least three numbers entered until the end of line.  We will consider repeated values as separate maxima.
    /// </summary>
    public class ThirdMaximum
    {
        public static void Main()
        {
            // Enter at least three numbers until the end of line.
            // Print out the third largest number at the end.

           
        }
    }
}

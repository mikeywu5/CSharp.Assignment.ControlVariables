using System;
using System.Collections.Generic;
using System.Text;

namespace Basic.MaxValue
{
    /// <summary>
    /// Print out the largest gap (a whole number) of two adjacent numbers entered until the end of line is reached. Print out -1 if less than two numbers are entered.
    /// </summary>
    public class LargestGap
    {
        public static void Main()
        {
            // Write codes to enter a sequence of integers until the end of line has been reached.
            // In the end, print out the largest difference between the two adjacent numbers entered (in integer).
        }
    }
}
